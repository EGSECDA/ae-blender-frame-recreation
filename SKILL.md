---
name: ae-blender-frame-recreation
description: 根据参考视频逐帧拆解并制作可编辑的 After Effects 动效工程，结合图像模型生成素材、Blender 三维场景与运镜，复刻短帧 MG、UI 和三维文字；适用于参考复刻、主题改编及已有成片的视觉返修。
---

# AE / Blender 逐帧视觉复刻

交付能继续编辑的 AE 工程、必要的 Blender 源文件、完整素材和经过验证的成片。先分清参考的节奏、图形语言、空间运动和可替换的题材；保留用户选定的软件与改编范围。

## 接手现有项目

先找实际输入视频、最新 AEP、生成脚本、素材清单、已导出版本和用户指出的帧。读取最新交付记录并验证文件存在，不能仅凭文件名的 FINAL / V3 判断版本。保护当前未保存的应用会话，脚本构建和后台渲染优先使用独立进程与唯一候选文件。

确认帧率、时长、画幅、音轨、主题、必要角色及输出目录。能从现有工程或用户上下文确定的事项直接沿用。素材、缓存、工程、渲染和审核证据分别放入项目子目录；运行环境与工具路径现场发现，不沿用他人机器的绝对路径。

## 按需要读取

- 参考拆解、连续帧提取、版本差异和编码检查：读 [frame-review.md](references/frame-review.md)，使用 `scripts/frame_review.py`。
- 原生 AE 编程、文字、人物框、三维图层、保存和渲染：读 [ae-engineering.md](references/ae-engineering.md)，按需载入 `scripts/ae_primitives.jsx`。
- 真实近中远景、纸张材质、镜头景深、透明序列：读 [blender-depth.md](references/blender-depth.md)。`scripts/blender_paper_demo.py` 是可编辑的最小演示，按用户场景重建布局。
- 素材生成和设计判断：读 [visual-direction.md](references/visual-direction.md)。
- 最终文件、版本冻结和故障处理：读 [delivery.md](references/delivery.md)。

## 工作循环

1. **逐帧建账。** 对参考的相关区段提取全部帧，记录镜头区间和内部 1–6 帧事件；完整复刻时覆盖整片。以整数帧保存事件，秒数用于显示。分别记录预备、冲击、过冲、落定、保持、退出，不用平滑长补间替代短促切换。示例结构见 [example_timeline.json](assets/example_timeline.json)，其数值仅是演示。
2. **先明确构图。** 给脸部、主要动作、标题、辅助文字和景深物体分配空间；设计明确的阅读优先级。人物、前景和文字可以有经过设计的遮挡，但脸和需要读完的文字应有完整可读阶段。将用户截图中的问题定位到连续帧范围，检查是否影响同类段落。
3. **分别制作。** 图像模型负责角色原画、环境画、材质等位图；可编辑 UI、可读文字、蒙版和 MG 在 AE 中制作；真实场景几何、物体深度、相机运动和景深在 Blender 中制作。生成图片不能代替用户要求的真实三维运镜。
4. **分阶段原生渲染。** 先做短测试确认字体、抠像、Alpha、模糊角度和时间映射，再制作全段。文字进场、角色框、对白、三维字和高潮分别检查；必要时并行分段审核，但整合工程只由一个写入者串行保存。
5. **看实际输出再修。** 修改关键帧或图层后，保存唯一候选，关闭重开核对关键参数，再从这个明确路径渲染。检查连续帧与正常速度播放各自能发现的问题，记录真正看过的范围。已有错误不能由“脚本 SUCCESS”关闭。
6. **冻结并交付。** 改动帧全部复看；只有像素或有效参数未变的区段才能沿用旧检查结论。完成原生工程检查、最终编码全片解码及音轨验证，整理正式入口和逐帧证据。不要把审核中间件冒充成片，也不要用“100% 信心”代替可核验结果。

## 三个容易做错的决定

- **对话是一个共同场景。** 多角色对话保持各自固定站位；发言状态控制彩色/灰色、亮度和轻微动作，避免靠每次替换同一个角色图层改变站位。左右安排服从当前用户要求。
- **固定框与内部取景分开。** 用窗口预合成裁切人物，外层移动窗口，内层移动/缩放原画。逐帧检查脸的实际位置，不能只检查图层中心或最终姿态。
- **高潮先有组织，再打破组织。** 例如规则符纸阵先聚集和短暂停顿，再在几帧内向镜头爆散，随后大幅减速。近景尺寸、虚焦、曲率、遮挡和中景清晰纹理共同表现深度；单纯增加平面图形数量只会加乱。

## 完成判断

用户指定的效果和修正已进入实际渲染；角色与关键文字在可读阶段清楚；短帧转场无意外跳动、空帧或蒙版泄漏；三维物体有真实深度且材质在交付分辨率下可辨；素材无丢失、表达式无错误、音轨和总帧数正确。对无法控制的窗口或未验证的项目如实说明，保持已有文件可用。
